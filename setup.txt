//bycrypt for pass:npm install -D @types/bcryptjs


// must be first to put in the page to render
export default function SoldItemsPage() {
  return <div>Sold Items</div>;
}


//npm install --save-dev @types/papaparse 
if component not found declarations
